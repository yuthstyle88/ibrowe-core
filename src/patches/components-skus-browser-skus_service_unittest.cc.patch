diff --git a/components/skus/browser/skus_service_unittest.cc b/components/skus/browser/skus_service_unittest.cc
index 745380b1c2ef5c0995597a19020204dead90e3e9..0b5c986a642ac8523c32ca80f1e188e6481e9826 100644
--- a/components/skus/browser/skus_service_unittest.cc
+++ b/components/skus/browser/skus_service_unittest.cc
@@ -312,9 +312,9 @@ TEST_F(SkusServiceTestUnitTest, CredentialSummaryFailed) {
 
 TEST_F(SkusServiceTestUnitTest, CredentialSummaryWrongEnv) {
   base::Value::Dict state;
-  auto testing_payload = GenerateTestingCreds("vpn.brave.software");
+  auto testing_payload = GenerateTestingCreds("vpn.ibrowe.software");
   state.Set("skus:staging", testing_payload);
   prefs()->SetDict(skus::prefs::kSkusState, std::move(state));
-  auto credentials = GetCredentialsSummary("vpn.brave.software");
+  auto credentials = GetCredentialsSummary("vpn.ibrowe.software");
   EXPECT_EQ(credentials, "{}");
 }
